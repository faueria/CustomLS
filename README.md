# CustomLS
Custom command line ls program which mimics some aspects of the original ls program. It is implemented with multiple available flags, including: long, recursive, human readable, count only, and all. It is able to recognize symlinks files/directories.
